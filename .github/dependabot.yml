version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 3
    labels:
      - dependencies
      - github-actions
    groups:
      github-actions:
        patterns:
          - "*"

  - package-ecosystem: npm
    directory: /
    schedule:
      interval: monthly
    # Keep npm version-update PRs paused until the Bun lockfile update path is
    # stable in CI. Dependabot security updates remain enabled in repo settings.
    open-pull-requests-limit: 0
    labels:
      - dependencies
      - javascript
    groups:
      npm-minor-and-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
